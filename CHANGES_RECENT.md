CHANGES (recent pull)
Date: 2026-04-21

Summary
- Fast-forwarded to commit `29abf7f` (2026-04-21): "Fix Telegram billing automation, improve GST approval workflow, and update security policies".
- Key changes: improvements to Telegram bot billing automation, GST approval workflow refinements, security/policy updates, docs/runbook updates, removal of tracked `.genkit` telemetry artifacts, and small service/tool refactors.
- Files touched (high level):
  - `bin/telegram_bot.dart` (billing & session handling)
  - `bin/genkit_ui_server.dart` (removed/modified)
  - `lib/services/approval_formatter.dart`, `lib/services/gst_calculator.dart`
  - `lib/tools/approval_tools.dart`, `lib/tools/billing_tools.dart`
  - `.gitignore`, `pubspec.yaml`
  - `start.sh` (added)

Details
- Commits included (most recent first):
  - `29abf7f` 2026-04-21 — Fix Telegram billing automation, improve GST approval workflow, and update security policies
  - `3b86d30` 2026-04-21 — GST AND DRAFTING
  - `21aa090` 2026-04-20 — feat(admin): add phase 1 schema/api and phase 2 flutter dashboard
  - `cae5647` 2026-04-19 — feat: phase 4.5 tools, security policies, and no-code dashboard
  - `1c2cd88` 2026-04-19 — Remove tracked .genkit telemetry artifacts from repository

Notes / Next steps
- Run `dart analyze` and test suite to validate runtime changes.
- Review `bin/telegram_bot.dart` for billing automation behaviors and environment expectations.
- If you have rotated credentials recently, ensure `.env` and deployment secrets are updated accordingly.

Generated by GitHub Copilot assistant.

---

Startup verification update
Date: 2026-04-22

- `start.sh` now auto-detects the installed Dart SDK and prefers the repo-local `.tooling/dart-sdk`.
- `start.sh` now launches `bin/genkit_ui.dart` for the real localhost app instead of the non-listening dev stub.
- `bin/genkit_ui.dart` now binds to `127.0.0.1` so `http://localhost:4000` works reliably on Linux.
- Verified services:
  - UI: `http://127.0.0.1:4000` and `http://localhost:4000`
  - Telegram bot: running via `bin/telegram_bot.dart`

Implementation update
Date: 2026-04-22

- Added invoice PDF generation for approved drafts in `lib/services/invoice_pdf_generator.dart`.
- PDF output supports separate layouts for IGST, CGST/SGST, non-GST, and composite GST cases.
- Telegram approval flow now sends the generated PDF document immediately after a bill draft is approved.
- Added focused tests for PDF template selection and verified the touched files with `dart test` and `dart analyze`.

PDF rendering polish
Date: 2026-04-22

- Embedded a Unicode-capable font in the invoice PDF renderer so the `₹` symbol renders correctly instead of showing fallback glyph boxes.
- Kept the rest of the invoice layout unchanged while improving currency readability on mobile viewers.